caselawld
=========

CaseLaw Linked Data.

After cloning you need to get the submodule at two levels:

git submodule init
git submodule update
cd fusepool-platform
git submodule init
git submodule update
